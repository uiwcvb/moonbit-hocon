export default {"slug": "hocon", "title": "HOCON 配置解析器", "scope": "嵌套/点路径、覆盖合并、内存 include 与替换解析", "source": "https://github.com/lightbend/config/blob/main/HOCON.md", "limitations": "标量保留为 Text；不含数组、隐式字符串拼接、可选/环境变量替换、三引号、URL/磁盘 include；include 必须由调用方显式提供内存映射。", "recommendation": "优先：include/替换/合并有实际使用场景，需补值类型与官方规范用例。", "version": "0.1.0", "example": "service { host = \"localhost\", port = 8080 }\nservice.port = 9090\nendpoint = ${service.host}"};
